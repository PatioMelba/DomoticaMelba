<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

	<style name="LoginStyle" parent="android:Theme.Material.Light.NoActionBar.Fullscreen">
       		 <!-- Used for the bottom line when not selected / focused -->
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:paddingBottom">24dp</item>
        <!-- colorControlActivated & colorControlHighlight use the colorAccent color by default -->
    </style>

    <style name="BorisStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/MainColor</item>
        <item name="titleTextColor">@color/white</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="android:actionBarStyle">@style/actionbar_style</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="actionbar_style" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/MainColor</item>
    </style>

    <style name="tab_layout_style" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/tab_style</item>
        <item name="tabSelectedTextColor">@color/white</item>
    </style>

    <style name="tab_style" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
